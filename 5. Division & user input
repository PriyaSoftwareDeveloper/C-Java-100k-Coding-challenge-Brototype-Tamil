#include <stdio.h>
#include <stdlib.h>

int main(void) {
		int num1, num2;
		float result;
	    setbuf(stdout, NULL);
	    printf("Enter two numbers: ");
	    scanf("%d%d", &num1, &num2);
	    result = num1/num2;
	    printf("%f", result);
	    return EXIT_SUCCESS;
}
