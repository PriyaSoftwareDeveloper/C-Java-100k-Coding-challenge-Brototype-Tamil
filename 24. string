#include <stdio.h>
#include <stdlib.h>

int main(void) {
	char name[20];
	setbuf(stdout, NULL);
	printf("Enter a Name:");
	scanf("%s", name);
	printf("Entered Name is: %s", name);
	return EXIT_SUCCESS;
}
